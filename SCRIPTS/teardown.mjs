#!/usr/bin/env node

// Lore OS teardown — run by the harness before removal.
// Data at ~/LORE-OS/ is preserved. Remove manually if no longer needed.

console.error("Lore OS removed. Data preserved at ~/LORE-OS/.");
console.error("Remove manually if no longer needed: rm -rf ~/LORE-OS");
